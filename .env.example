# 复制成 .env 后填写（.env 已被 .gitignore 忽略，永不入库）。
# 也可以不用文件，直接把这两个变量 export 到环境里。

# 你的 Canvas 地址。三种常见形态都能自动补 https://：
#   托管版 : your-school.instructure.com
#   自建版 : canvas.your-school.edu
CANVAS_HOST=""

# 访问令牌：Canvas → 账户(Account) → 设置(Settings) → Approved Integrations
#   → New Access Token → 生成后立刻复制（只显示一次）。
# 它等于你的密码，别贴进聊天、别提交、别截图发群里。
CANVAS_API_TOKEN=""

# 可选：默认学期，仅用于你自己过滤课程，工具本身不依赖它。
CANVAS_TERM=""
